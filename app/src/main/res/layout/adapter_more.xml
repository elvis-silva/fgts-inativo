<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:background="@drawable/adapter_bg">

    <ImageView
        android:id="@+id/adapter_more_thumbnail"
        android:layout_width="?android:attr/listPreferredItemHeight"
        android:layout_height="?android:attr/listPreferredItemHeight"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:layout_marginEnd="6dp"
        android:contentDescription="@string/todo"
        android:src="@drawable/btn_bg" />

    <!--TextView
        android:id="@+id/credits"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/thumbnail"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="@string/credits"
        android:textSize="12sp" /-->

    <TextView
        android:id="@+id/adapter_more_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignBottom="@+id/adapter_more_thumbnail"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/adapter_more_thumbnail"
        android:layout_toEndOf="@id/adapter_more_thumbnail"
        android:gravity="center_vertical"
        android:textSize="16sp"
        android:textStyle="bold"
        android:text="@string/text_text_text"
        android:textColor="@color/colorPrimaryDark"/>
</RelativeLayout>